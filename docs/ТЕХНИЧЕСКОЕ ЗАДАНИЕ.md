# ТЕХНИЧЕСКОЕ ЗАДАНИЕ

## Система обнаружения, визуализации, мониторинга и анализа физической L2-топологии сети

**Статус документа:** базовая спецификация продукта  
**Целевые платформы:** legacy Windows 8.1 / Windows Server 2012 и новее; modern backend — поддерживаемые Windows и Linux
**Основной язык интерфейса:** русский  
**Основной язык разработки:** C#  
**Целевая среда выполнения:** .NET Framework 4.8 (legacy); .NET Standard 2.0 (shared core); modern Engine — .NET 8.0 сейчас, целевой production runtime — .NET 10 LTS после обновления toolchain
**Тип приложения:** WPF Windows-клиент + backend host: legacy NetLoom.Service или modern NetLoom.Engine для Windows/Linux

---

# 1. Назначение системы

Разрабатываемая система предназначена для автоматизированного обнаружения, документирования, визуализации, мониторинга и диагностики физической и логической топологии Ethernet-сетей.

Основная задача системы — дать оператору достоверный и понятный ответ на вопросы:

- какие сетевые устройства существуют;
- где физически или логически находится каждое устройство;
- к какому устройству и к какому порту оно подключено;
- какой тип соединения используется;
- какие физические кольца существуют в сети;
- какие порты участвуют в RSTP/STP;
- какие линии в настоящий момент находятся в состоянии Forwarding;
- какие линии заблокированы Spanning Tree;
- какой Bridge является Root Bridge;
- как изменилась топология перед аварией;
- где последний раз наблюдался конкретный IP или MAC;
- какие сведения являются подтверждёнными, а какие построены только на косвенных признаках.

Система не должна позиционироваться как универсальная замена Zabbix, PRTG, LibreNMS или других систем общего мониторинга.

Основная специализация продукта:

**физическая L2-топология + LLDP/CDP + FDB/ARP-корреляция + STP/RSTP + анализ колец + история изменения связей.**

---

# 2. Основные принципы архитектуры

При разработке должны соблюдаться следующие архитектурные принципы.

## 2.1. Наблюдение не является фактом топологии

Полученная по SNMP запись не должна автоматически становиться физической связью.

Между сетевыми коллекторами и итоговой картой должен существовать слой наблюдений:

```text
Сеть
  ↓
Коллекторы
  ↓
Наблюдения / Evidence
  ↓
Topology Resolver
  ↓
Нормализованная модель сети
  ↓
Карта
```

Например:

```text
MOXA-84 сообщает:

LocalPort = 8
RemoteChassisId = 00:11:22:33:44:55
RemotePort = sfp1
```

Система первоначально должна сохранить это как наблюдение LLDP.

Только Topology Resolver должен определить:

```text
Устройство A / Port8
        ↕
Устройство B / sfp1
```

и присвоить связи уровень достоверности.

---

# 3. Основные сущности системы

Система должна работать как минимум со следующими логическими сущностями:

- Device;
- DeviceIdentity;
- ManagementEndpoint;
- Interface;
- Location;
- Link;
- LinkEvidence;
- Observation;
- LldpObservation;
- CdpObservation;
- FdbObservation;
- ArpObservation;
- StpObservation;
- HealthObservation;
- AccessProfile;
- MonitoringSchedule;
- NetworkEvent;
- TopologySnapshot;
- UserAnnotation;
- ApplicationSetting.

---

# 4. Идентичность устройства

IP-адрес запрещается использовать как постоянный первичный идентификатор устройства.

Каждое обнаруженное устройство получает внутренний:

```text
DeviceId = GUID
```

IP является одним из изменяемых атрибутов.

Для идентификации устройства необходимо использовать набор Identity Claims:

- serial number;
- LLDP Chassis ID;
- chassis MAC;
- management MAC;
- SNMP sysObjectID;
- SNMP sysName;
- hostname;
- management IP;
- другие доступные идентификаторы.

Пример:

```text
DeviceId:
4c698338-...

CustomName:
Коммутатор серверной

ManagementIP:
10.48.228.84

sysName:
MOXA-09801

LLDP Chassis ID:
00:90:E8:...

Serial:
ABCD12345
```

Следует учитывать, что LLDP Chassis ID не обязан быть MAC-адресом.

Он может представлять:

- MAC;
- network address;
- interface name;
- locally assigned identifier;
- другой тип идентификатора.

При конфликте Identity Claims запрещается автоматически объединять устройства без достаточных оснований.

Должны существовать ручные операции:

- «Объединить устройства»;
- «Разделить ошибочно объединённое устройство».

Пользовательское название устройства должно сохраняться независимо от:

- изменения IP;
- изменения sysName;
- перезагрузки;
- временной недоступности.

---

# 5. Типы устройств

Система должна поддерживать следующие логические категории:

- управляемый коммутатор;
- маршрутизатор;
- firewall;
- криптографический шлюз;
- сервер;
- рабочая станция;
- терминальный сервер;
- PLC/RTU;
- точка доступа;
- принтер;
- IP-камера;
- медиаконвертор;
- unmanaged switch;
- оптический преобразователь;
- passive/network equipment;
- неизвестное устройство;
- пользовательский тип.

Категория должна определять только оформление и некоторые правила обработки.

Категория не должна жёстко определять возможности устройства.

---

# 6. Ручные неуправляемые устройства

Система обязана поддерживать ручное создание устройств, которые невозможно обнаружить средствами SNMP, LLDP, CDP или IP.

Это требуется, например, для:

- тупых медиаконверторов;
- unmanaged switch;
- оптических преобразователей;
- пассивных сетевых устройств;
- промежуточного оборудования;
- неизвестных соединительных устройств.

Пользователь должен иметь возможность:

- создать такое устройство;
- присвоить ему имя;
- выбрать тип;
- выбрать форму отображения;
- указать число виртуальных портов;
- дать портам названия;
- вручную нарисовать соединения;
- указать тип каждого соединения;
- добавить комментарий;
- указать объект размещения.

Ручное устройство получает:

```text
DiscoverySource = Manual
MonitoringCapability = None
```

Оно не должно автоматически считаться Offline.

Статус такого устройства:

```text
Не контролируется
```

или:

```text
Нет данных
```

Ручные устройства и ручные линии обязательно должны участвовать в алгоритме поиска физических циклов.

Таким образом, вручную добавленный медиаконвертор может стать частью обнаруженного физического кольца.

Ручной элемент запрещается удалять в результате автоматического Discovery.

---

# 7. Объекты и физическое размещение

Для повышения читаемости карты должна существовать отдельная система объектов размещения.

Примеры:

```text
Площадка
 ├── Офис 1
 │    ├── Серверная
 │    └── Кабинет 101
 │
 ├── Офис 2
 │    ├── Серверная
 │    └── Узел связи
 │
 └── Котельная
```

Система должна поддерживать вложенные Location.

Минимальные типы:

- площадка;
- объект;
- здание;
- помещение;
- серверная;
- шкаф;
- пользовательская группа.

На карте Location отображается как крупная прямоугольная область с подписью.

Например:

```text
┌──────────────────────── ОФИС 1 ────────────────────────┐
│                                                        │
│   [SW-01] ───────── [MOXA-01]                         │
│                                                        │
│                      [Server-01]                       │
│                                                        │
└────────────────────────────────────────────────────────┘
```

Функции объекта:

- изменение размера;
- перемещение;
- переименование;
- цвет/фон;
- комментарий;
- блокировка положения;
- сворачивание;
- разворачивание;
- вложенность;
- скрытие.

При перемещении объекта пользователь может выбрать:

```text
Переместить только рамку
```

или:

```text
Переместить рамку вместе с устройствами
```

Устройство может принадлежать одному основному Location.

Линии между различными Location должны оставаться видимыми.

---

# 8. Визуальная карта

Главный интерфейс системы должен представлять собой простую псевдографическую карту.

Не требуется сложная трёхмерная или дизайнерская визуализация.

Основные элементы:

- прямоугольники;
- скруглённые прямоугольники;
- круги;
- простые иконки;
- линии;
- текстовые подписи;
- рамки объектов.

Цель интерфейса:

**максимальная информативность при минимальной визуальной сложности.**

---

# 9. Узлы карты

Узел должен отображать как минимум:

```text
Пользовательское имя
IP
sysName или модель
Статус
```

Пример:

```text
┌─────────────────────┐
│ Коммутатор Офис 1   │
│ 10.48.228.84        │
│ MOXA EDS-408A       │
└─────────────────────┘
```

Допускаются различные формы:

- квадрат;
- прямоугольник;
- круг;
- скруглённый прямоугольник;
- пользовательский простой символ.

Форму допускается назначать:

- автоматически;
- вручную.

---

# 10. Цветовые состояния устройства

Цвет не должен быть единственным способом передачи статуса.

Всегда должна существовать текстовая расшифровка.

Предусмотреть следующие итоговые состояния:

```text
Доступен
Частично доступен
Наблюдается косвенно
Недоступен
Нет данных
Мониторинг отключён
Не контролируется
```

При этом внутренне должны отдельно храниться:

```text
ICMP
SNMP
TCP
LLDP Seen
CDP Seen
FDB Seen
ARP Seen
LastSeen
```

Например:

```text
ICMP: недоступен
SNMP: доступен
TCP/443: доступен
```

не должно приводить к статусу:

```text
Устройство недоступно
```

---

# 11. Редактирование карты

Пользователь должен иметь возможность:

- перемещать устройство мышью;
- перемещать группы;
- создавать ручные узлы;
- создавать ручные линии;
- удалять ручные элементы;
- редактировать подписи;
- изменять пользовательское название;
- добавлять заметки;
- изменять форму;
- изменять размер;
- фиксировать положение;
- скрывать элемент;
- показывать скрытые элементы;
- масштабировать карту;
- перемещать рабочую область.

Координаты устройств сохраняются в БД.

Автоматическое обновление топологии запрещается использовать как повод для изменения положения уже существующих узлов.

---

# 12. Автоматическое размещение

Сложная автоматическая компоновка графа не является обязательной функцией MVP.

Для нового устройства используется простая стратегия:

1. найти известного физического соседа;
2. разместить новое устройство рядом с ним;
3. проверить наличие свободной области;
4. если сосед неизвестен — поместить устройство в область «Новые устройства».

Должна существовать функция:

```text
Зафиксировать расположение
```

После фиксации автоматический Discovery не имеет права перемещать существующие элементы.

---

# 13. Обнаружение устройств

Discovery не должен основываться только на ICMP.

ICMP является лишь одним из источников наблюдений.

Поддерживаются:

- seed-IP;
- диапазон CIDR;
- ручной IP;
- SNMP;
- разрешённый TCP Connect;
- LLDP-соседи;
- CDP-соседи;
- ARP;
- FDB;
- DNS;
- ручное добавление.

Пример:

```text
10.48.228.0/24
```

может быть профилем Discovery.

---

# 14. Безопасность Discovery

Категорически запрещается:

- автоматически перебирать SNMP community strings;
- подбирать пароли;
- автоматически проверять стандартные credentials;
- выполнять SNMP SET;
- выполнять произвольные команды;
- запускать агрессивное сканирование без явного разрешения оператора.

Система представляет собой диагностический read-only инструмент.

---

# 15. Access Profile

Все активные методы взаимодействия задаются через AccessProfile.

Пример:

```text
Название:
АСУ ТП

Сеть:
10.48.228.0/24

ICMP:
Да

SNMP v2c:
Да

Community:
********

TCP Connect:
443, 22

Timeout:
1500 мс

Retries:
2

Parallel Requests:
5
```

Профиль содержит:

- название;
- IP/CIDR;
- SNMP version;
- credentials;
- разрешённые методы;
- разрешённые TCP-порты;
- timeout;
- retries;
- maximum parallelism;
- минимальную задержку между запросами;
- exclusions;
- расписание.

---

# 16. SNMP

Обязательная поддержка:

- SNMP v1;
- SNMP v2c;
- SNMP v3.

Основные стандартные данные:

- sysDescr;
- sysObjectID;
- sysUpTime;
- sysName;
- sysLocation;
- ifTable;
- ifXTable;
- BRIDGE-MIB;
- LLDP-MIB;
- доступные RSTP/STP MIB;
- ARP/neighbor tables;
- FDB tables.

Опрос должен использовать только необходимые OID.

Полный SNMP Walk не должен выполняться постоянно без необходимости.

---

# 17. LLDP

LLDP является основным источником физической L2-топологии.

Необходимо сохранять:

- local chassis;
- local port;
- remote chassis;
- remote port;
- remote sysName;
- remote port description;
- management address;
- время наблюдения;
- исходные raw значения.

Следует учитывать, что LLDP local port number не обязан совпадать с ifIndex.

Необходимо выполнить корректное сопоставление LLDP port ↔ Interface.

При невозможности однозначного сопоставления результат помечается соответствующей достоверностью.

---

# 18. CDP

Для Cisco должна поддерживаться работа с CDP.

Сохраняются:

- remote Device ID;
- remote port;
- local interface;
- management address;
- platform;
- capabilities;
- timestamp.

CDP и LLDP рассматриваются как независимые источники evidence.

---

# 19. FDB

Система должна собирать MAC forwarding tables.

Важно:

```text
MAC виден на Port8
```

не означает:

```text
MAC физически подключён непосредственно к Port8
```

Следовательно, FDB Observation хранится отдельно от PhysicalLink.

Пример:

```text
MAC:
00:11:22:33:44:55

SeenBehind:
MOXA-84 / Port8
```

Только Topology Resolver имеет право использовать эту информацию для inference.

---

# 20. Mapping Bridge Port → ifIndex

FDB и STP используют bridge-port index.

Категорически запрещается предполагать:

```text
BridgePortIndex == ifIndex
```

Для сопоставления необходимо использовать:

```text
dot1dBasePortIfIndex
```

Алгоритм:

```text
Bridge Port
     ↓
dot1dBasePortIfIndex
     ↓
ifIndex
     ↓
Interface
```

Это правило является обязательным для всех соответствующих модулей.

---

# 21. ARP/IP correlation

Система должна уметь связывать:

```text
IP
 ↓
ARP
MAC
 ↓
FDB
Switch / Port
```

Это особенно необходимо для оборудования:

- Cisco ASA;
- С-Терра;
- закрытых маршрутизаторов;
- серверов;
- устройств с отключённым ICMP;
- устройств без доступного SNMP.

Результат должен отображаться как косвенное наблюдение.

Например:

```text
10.48.228.15
MAC: xx:xx:xx:xx:xx:xx
виден за MOXA-84 / Port3

Достоверность:
Предполагаемая
```

---

# 22. Модель достоверности

У связи должны существовать отдельно:

## Strength

```text
Confirmed
Observed
Inferred
Manual
```

## Freshness

```text
Current
Stale
```

`Stale` не является источником связи.

Например:

```text
Strength = Confirmed
Freshness = Stale
```

означает:

«Связь ранее была подтверждена LLDP с двух сторон, но давно не подтверждалась».

---

# 23. Confirmed Link

Связь считается Confirmed, если имеется достаточно согласованных наблюдений.

Наиболее надёжный вариант:

```text
A говорит:
A/P1 → B/P5

B говорит:
B/P5 → A/P1
```

Topology Resolver объединяет два observations в одну PhysicalLink.

---

# 24. Observed Link

Односторонний LLDP/CDP:

```text
A/P1 → B/P5
```

без подтверждения с B.

Такая связь существует на карте, но должна иметь статус:

```text
Observed
```

---

# 25. Inferred Link

Связь, построенная на основе:

- FDB;
- ARP;
- OUI;
- комбинации нескольких таблиц;
- косвенного анализа,

получает:

```text
Inferred
```

Такая линия визуально отличается от подтверждённой.

---

# 26. Manual Link

Пользователь может вручную зафиксировать:

```text
MOXA Port8
    ↕
MediaConverter Port1
```

или любую другую связь.

Она получает:

```text
Strength = Manual
```

Автоматический Discovery не должен её удалять.

При обнаружении противоречия система должна показать предупреждение оператору.

---

# 27. STP / RSTP

Система должна собирать и сохранять Spanning Tree observations.

Минимально:

- Bridge ID;
- Root Bridge ID;
- root path cost;
- topology changes;
- bridge port;
- mapped ifIndex;
- state;
- timestamp.

Поддерживаемые состояния:

```text
Disabled
Blocking
Listening
Learning
Forwarding
Broken
Unknown
```

Для пользовательского интерфейса допускается нормализованное название:

```text
Blocking / Discarding
```

при обязательном сохранении исходного raw значения.

---

# 28. Port Role

Port Role должен собираться только если он действительно предоставляется соответствующим стандартным или vendor-specific MIB.

При отсутствии данных:

```text
Role = Unknown
```

Запрещается выдавать предположение за фактическую роль.

---

# 29. MSTP-ready архитектура

Состояние STP запрещается хранить как единственное:

```text
Interface.RstpState
```

Необходимо использовать сущность:

```text
StpObservation
```

с полями:

```text
DeviceId
InterfaceId
BridgePortIndex
Protocol
InstanceId
PortState
PortRole
RootBridgeId
DesignatedBridgeId
PathCost
ObservedAt
RawValue
```

Даже если первая версия работает только с CIST/RSTP, архитектура должна позволять позднее добавить MSTI.

---

# 30. Анализ физических колец

Topology Analyzer должен рассматривать физическую топологию как граф:

```text
G = (Devices, PhysicalLinks)
```

Необходимо реализовать поиск циклов.

Каждое найденное кольцо получает:

```text
RingId
```

и перечень:

- устройств;
- интерфейсов;
- линий.

Пример:

```text
Кольцо №3

MikroTik-146/SFP
→ MOXA-84/P8
→ MikroTik-147/SFP
→ MikroTik-146/SFP
```

Ручные устройства и ручные связи также входят в этот граф.

---

# 31. Анализ защиты кольца

Для каждого обнаруженного физического цикла система должна попытаться определить:

- есть ли STP/RSTP;
- существует ли Blocking/Discarding port;
- кто Root Bridge;
- существует ли несколько потенциальных blocking points;
- существует ли кольцо, в котором все известные порты находятся Forwarding.

Последняя ситуация является важным предупреждением.

Пример:

```text
ПРЕДУПРЕЖДЕНИЕ

Обнаружено физическое кольцо №4.

Ни на одном из доступных портов
не обнаружено состояние Blocking/Discarding.
```

При недостатке данных сообщение должно говорить:

```text
Невозможно подтвердить наличие защиты от петли.
```

а не утверждать наличие аварии.

---

# 32. Режимы карты

Карта должна иметь как минимум три режима.

## Физическая топология

Показываются все известные физические соединения.

## Активное STP-дерево

Основной акцент делается на Forwarding path.

Blocking/Discarding links отображаются отдельным стилем.

## Достоверность

Линии оформляются в зависимости от:

```text
Confirmed
Observed
Inferred
Manual
Stale
```

---

# 33. Подписи линий

Линия может отображать:

```text
Port8 ↔ sfp1
Оптика
1 Гбит/с
RSTP: Blocking
```

Обязательные данные:

- local port;
- remote port.

Опционально:

- media;
- speed;
- VLAN;
- STP state;
- confidence.

Количество отображаемой информации должно регулироваться настройкой карты.

---

# 34. Тип линии

Поддерживаются:

```text
Медь
Оптика
Беспроводная
Логическая
Неизвестно
Пользовательский тип
```

Автоматическое определение типа линии рассматривается как предположение.

Пользователь имеет право вручную исправить тип.

Ручное значение имеет приоритет для визуализации, но автоматическое observation сохраняется отдельно.

---

# 35. Мониторинг

Мониторинг не должен обязательно работать постоянно.

Это принципиальное требование.

Должны существовать четыре режима:

```text
Отключён
Только вручную
По расписанию
Постоянный периодический
```

---

# 36. Глобальное управление мониторингом

В основном интерфейсе должны присутствовать кнопки:

```text
Запустить мониторинг
Остановить мониторинг
Опросить сейчас
Обновить топологию сейчас
```

При отключении мониторинга система прекращает активные опросы оборудования.

Сохранённая карта остаётся доступной.

Статусы не должны автоматически становиться красными.

Они получают признак:

```text
Мониторинг остановлен
```

с отображением времени последнего опроса.

---

# 37. Расписание мониторинга

Пользователь должен иметь возможность настроить, например:

```text
Понедельник–Пятница
08:00–18:00
```

или:

```text
Каждый день
02:00
Topology Discovery
```

или:

```text
Health Check
каждые 5 минут
только с 07:00 до 19:00
```

Расписание сохраняется и работает независимо от открытого UI.

---

# 38. Раздельные циклы мониторинга

Должно существовать не менее трёх классов задач.

## Health Check

Быстрый.

Например:

- ICMP;
- sysUpTime;
- минимальный SNMP GET;
- разрешённые TCP checks.

## Interface Monitoring

Средняя частота.

Например:

- ifOperStatus;
- ifAdminStatus;
- ошибки;
- выбранные интерфейсные counters.

## Topology Discovery

Редкий.

Например:

- LLDP;
- CDP;
- FDB;
- ARP;
- STP;
- identity reconciliation.

Каждый класс имеет собственное расписание.

---

# 39. Мониторинг отдельных устройств и объектов

Мониторинг должен включаться/выключаться:

- глобально;
- для AccessProfile;
- для объекта;
- для устройства.

Например:

```text
Офис 1:
мониторинг включён

Офис 2:
только ручной опрос

С-Терра:
активный опрос запрещён
```

---

# 40. Безопасное ограничение нагрузки

Для каждого AccessProfile:

- maximum parallel requests;
- timeout;
- retries;
- delay;
- schedule.

Глобально должен существовать maximum parallelism.

Для старого промышленного оборудования должны поддерживаться консервативные настройки.

Пример:

```text
Параллельных устройств:
3

Timeout:
2000 мс

Retries:
1

Topology Discovery:
раз в 6 часов
```

---

# 41. Работа с частичными ошибками

Ошибка одного OID не должна приводить к провалу всего устройства.

Например:

```text
sysName: OK
ifTable: OK
LLDP: timeout
RSTP: unsupported
```

Результат должен сохраняться частично.

---

# 42. Повторные попытки

Стратегия Retry должна быть управляемой.

Пример:

```text
Timeout = 1500 мс
Retries = 2
Delay = 300 мс
```

Необходимо избегать синхронного массового повторного опроса всех устройств.

Желателен небольшой jitter.

---

# 43. Устройства без ICMP

ICMP timeout не означает:

```text
Offline
```

Если доступны:

- SNMP;
- TCP;
- LLDP observation;
- FDB observation,

устройство отображается соответствующим образом.

---

# 44. Устройства без прямого доступа

Для Cisco ASA, С-Терра и аналогичного оборудования система может располагать только:

- IP;
- MAC;
- OUI;
- DNS name;
- соседним LLDP;
- FDB;
- ARP;
- ручным описанием.

Это считается допустимым состоянием.

Система не должна пытаться получить дополнительные сведения неразрешёнными методами.

---

# 45. Vendor Adapter

Специфика производителей инкапсулируется в отдельных адаптерах.

Минимально предусмотреть:

```text
GenericAdapter
MoxaAdapter
MikroTikAdapter
CiscoAdapter
CTerraAdapter
```

GenericAdapter всегда используется первым.

Vendor Adapter добавляет особенности, но не должен заменять стандартный путь без необходимости.

---

# 46. MOXA

Следует учитывать:

- различия между сериями;
- возможный Turbo Ring;
- различия прошивок;
- старые реализации SNMP;
- неполный LLDP;
- различные обозначения портов;
- возможные vendor MIB.

Нельзя автоматически предполагать наличие RSTP только по модели.

---

# 47. Turbo Ring

Поддержка анализа Turbo Ring рассматривается как отдельный vendor-specific модуль.

Если система точно определяет наличие Turbo Ring, она должна отображать это отдельно от стандартного RSTP.

Запрещается интерпретировать Turbo Ring как обычный RSTP без подтверждения.

---

# 48. MikroTik

Поддержать RouterOS и, по возможности, SwOS с учётом различий доступных MIB.

Vendor-specific код не должен попадать в общий Topology Resolver.

---

# 49. Cisco

Поддержать:

- LLDP;
- CDP;
- стандартный SNMP;
- доступные bridge/STP данные;
- Cisco-specific identification.

Cisco ASA не должна автоматически рассматриваться как обычный L2-коммутатор.

---

# 50. История

Необходимо разделять:

```text
Current State
```

и:

```text
History
```

Не требуется бесконечно сохранять каждое значение каждой метрики.

История должна в первую очередь хранить изменения.

---

# 51. Network Event

Минимальные типы событий:

```text
Устройство обнаружено
Устройство перестало отвечать
Устройство снова доступно
SNMP недоступен
SNMP восстановлен

Порт поднялся
Порт упал

Обнаружен новый сосед
Сосед исчез
Сосед на порту изменился

Создана новая физическая связь
Связь перестала подтверждаться

STP State changed
Root Bridge changed
Topology Change detected

MAC moved
Possible MAC flapping

Обнаружено новое кольцо
Кольцо исчезло
Изменилась точка блокировки кольца

Изменился management IP
Изменился sysName
```

---

# 52. Корреляция событий

Система должна позволять анализировать события в общей временной шкале.

Пример:

```text
12:41:02  Root Bridge changed
12:41:02  Topology Change
12:41:03  Port7 Forwarding → Blocking
12:41:03  MAC moved
12:41:04  10.48.228.147 SNMP timeout
12:41:08  Port7 Blocking → Forwarding
12:41:09  10.48.228.147 доступен
```

На основании последовательности система может формировать:

```text
Возможная корреляция событий
```

но не должна автоматически объявлять причинно-следственную связь без доказательств.

---

# 53. MAC flapping

При наличии последовательности быстрых перемещений одного MAC между портами должно генерироваться событие:

```text
Возможный MAC flapping
```

Порог и временное окно настраиваются.

---

# 54. Topology Snapshot

Система должна поддерживать снимки топологии.

Snapshot содержит состояние:

- Devices;
- Links;
- Locations;
- STP;
- Root Bridge;
- Ring list;
- confidence;
- timestamp.

Должна существовать функция сравнения двух snapshots.

Пример:

```text
Добавлено:
1 устройство
2 линии

Удалено:
1 линия

Изменено:
Root Bridge
3 состояния портов
```

---

# 55. Stale

Устройство или связь запрещается удалять после одного неудачного опроса.

Хранятся:

```text
FirstSeen
LastSeen
LastConfirmed
FailureCount
```

После заданного времени объект получает:

```text
Stale
```

Удаление:

- только вручную;
- либо отдельной политикой retention.

По умолчанию автоматическое физическое удаление запрещено.

---

# 56. Поиск

Поиск должен поддерживать:

- IP;
- MAC;
- hostname;
- sysName;
- CustomName;
- serial;
- vendor;
- model;
- interface.

Особая функция:

```text
Где находится MAC?
```

Должна показывать все последние FDB observations.

---

# 57. Карточка устройства

Карточка содержит:

## Общие сведения

- пользовательское имя;
- IP;
- sysName;
- sysDescr;
- sysObjectID;
- vendor;
- model;
- serial;
- Location.

## Доступность

- ICMP;
- SNMP;
- TCP;
- Last Seen.

## Интерфейсы

- название;
- ifIndex;
- status;
- speed;
- MAC;
- STP;
- neighbor.

## Identity

Все известные identity claims.

## History

История изменений.

## Notes

Пользовательские комментарии.

---

# 58. Карточка линии

Должна содержать:

- Device A;
- Port A;
- Device B;
- Port B;
- тип;
- скорость;
- состояние;
- STP state обеих сторон;
- evidence;
- Strength;
- Freshness;
- FirstSeen;
- LastSeen;
- LastConfirmed;
- пользовательскую заметку.

---

# 59. Карточка объекта

Объект физического размещения содержит:

- имя;
- тип;
- описание;
- список устройств;
- количество Online;
- количество проблем;
- количество Stale;
- комментарий.

---

# 60. Журнал

Система должна иметь два различных журнала.

## Технический журнал

Ошибки приложения:

- exception;
- timeout;
- parser error;
- DB error;
- SNMP error.

## Журнал сетевых событий

Изменения наблюдаемой сети.

Это разные сущности.

---

# 61. Экспорт

Поддержать:

```text
JSON
CSV
PNG
```

JSON должен позволять воспроизвести топологию.

CSV предназначен для инвентаризации.

PNG предназначен для документации.

В дальнейшем возможно добавить:

```text
SVG
PDF
Visio-compatible export
```

---

# 62. Импорт

Поддержать импорт:

- IP;
- CIDR;
- CSV inventory;
- JSON snapshot;
- пользовательских объектов.

---

# 63. Резервное копирование

Пользователь должен иметь функцию:

```text
Создать резервную копию
```

Сохраняются:

- SQLite database;
- configuration;
- карта;
- Location;
- пользовательские настройки.

Secrets при backup должны оставаться защищёнными.

---

# 64. Безопасность credentials

SNMP community и SNMPv3 credentials не должны храниться открытым текстом.

Использовать Windows DPAPI.

Секреты сохраняются службой под используемой service account.

UI не должен отображать сохранённый секрет в открытом виде.

---

# 65. Архитектура приложения

Целевая архитектура:

```text
NetLoom.Service.exe
        │
        ├── Scheduler
        ├── Discovery
        ├── SNMP Collector
        ├── LLDP/CDP Collector
        ├── FDB/ARP Collector
        ├── STP Collector
        ├── Health Monitor
        ├── Topology Resolver
        ├── Ring Analyzer
        ├── Event Engine
        └── SQLite
               ▲
               │
          Named Pipes (Windows transport implementation)
               │
               ▼
NetLoom.Wpf.exe
        │
        └── WPF
```

Фоновая служба должна работать без открытого UI.

---

# 66. MVP и первая реализация

Допускается первоначально разместить Service и UI внутри одного процесса для ускорения разработки.

Однако архитектурные интерфейсы должны исключать прямую зависимость:

```text
Collector → WPF
```

Collector должен возвращать model/domain objects.

UI является потребителем модели.

---

# 67. Технологический стек

Базовый:

```text
C#
.NET Framework 4.8
WPF
SQLite
SNMP library
Named Pipes (Windows transport implementation)
Task Parallel Library
SemaphoreSlim
```

Запрещается без необходимости увеличивать количество сторонних зависимостей.

Каждая новая dependency должна иметь техническое обоснование.

---

# 68. Работа с SQLite

SQLite является основной БД.

Обязательно:

- SchemaVersion;
- миграции;
- индексы;
- foreign keys;
- транзакции;
- backup;
- WAL при подтверждённой совместимости.

Схему БД нельзя менять без предварительного анализа возможности использовать существующие таблицы.

---

# 69. UI и БД

UI в конечной архитектуре не должен самостоятельно изменять SQLite.

Запись выполняет Service.

UI передаёт команды Service.

Это снижает риск:

- lock;
- race conditions;
- повреждения данных.

---

# 70. Производительность

Целевая начальная эксплуатация:

```text
до 500 устройств
до 5000 интерфейсов
до 10000 наблюдаемых связей/evidence
```

Система не обязана конкурировать с Zabbix на десятках тысяч устройств.

---

# 71. Рендеринг

Для небольших карт допустимы WPF Controls.

При увеличении количества линий рекомендуется:

- узлы — WPF Controls;
- линии — DrawingVisual или общий drawing layer.

Не следует создавать тысячи тяжёлых TextBlock/Line, если это начинает влиять на производительность.

---

# 72. Кэш

Collector должен поддерживать кэш текущего состояния.

UI обновляется только при изменении соответствующих свойств.

Topology Discovery должен быть инкрементальным.

---

# 73. Режим симуляции

Обязательная функция разработки.

Система должна уметь работать без реальной сети.

Рекомендуемый формат simulator:

не готовые объекты NetworkNode, а сохранённые raw responses.

Например:

```text
SNMP VarBind snapshot
LLDP snapshot
FDB snapshot
STP snapshot
```

Это позволяет тестировать настоящий parser и Topology Resolver.

---

# 74. Raw evidence

При диагностике сложных случаев должна существовать возможность посмотреть исходные данные.

Например:

```text
OID
Raw value
Timestamp
Source device
```

Это существенно облегчает поиск ошибок MIB и vendor-specific поведения.

---

# 75. Русский интерфейс

Абсолютно весь пользовательский интерфейс должен быть русскоязычным.

Русскими должны быть:

- меню;
- кнопки;
- сообщения;
- ошибки;
- предупреждения;
- статусы;
- настройки;
- справка;
- журнал сетевых событий.

Официальные технические названия сохраняются:

```text
LLDP
RSTP
SNMP
ifIndex
sysName
OID
Forwarding
```

При необходимости рядом указывается русская расшифровка.

---

# 76. Комментарии исходного кода

Все комментарии в собственном исходном коде проекта должны быть на русском языке.

Это относится к:

- `//`;
- `/* */`;
- XML documentation;
- пояснениям сложных алгоритмов;
- TODO/FIXME.

Идентификаторы классов, методов и переменных рекомендуется оставлять английскими.

Например:

```csharp
// Сопоставляем bridge-port с физическим ifIndex.
// Нельзя считать, что эти значения совпадают.
private int ResolveInterfaceIndex(...)
```

Это является обязательным coding rule.

---

# 77. Пользовательские строки

Пользовательские строки рекомендуется вынести в централизованные Resources.

Даже если в первой версии существует только русский язык.

Это исключит разбрасывание текстов по code-behind.

---

# 78. Ошибки

Программа не должна завершаться из-за:

- timeout одного устройства;
- неправильного SNMP ответа;
- отсутствующего OID;
- неизвестного vendor;
- неполной LLDP таблицы;
- неожиданного значения enum.

Ошибка должна локализоваться в конкретном observation.

---

# 79. Логирование ошибок

Каждая ошибка должна содержать:

```text
Timestamp
Device
Operation
Protocol
OID, если применимо
Exception
Attempt
```

Community/password запрещается писать в журнал.

---

# 80. Порядок разработки

Разработка выполняется последовательно.

## Версия 0.1 — Inventory Collector

Реализовать:

- AccessProfile;
- seed IP;
- CIDR;
- SNMP;
- identity;
- interfaces;
- JSON/CSV export;
- logging.

UI минимальный.

Критерий:

система достоверно собирает факты.

## Версия 0.2 — Topology Collector

Добавить:

- LLDP;
- CDP;
- FDB;
- ARP;
- Observation layer;
- Topology Resolver;
- LinkEvidence.

Критерий:

получается корректный список:

```text
Device A / Port A
↕
Device B / Port B
```

с достоверностью.

## Версия 0.3 — Карта

Добавить:

- WPF Canvas;
- узлы;
- линии;
- Location;
- drag-and-drop;
- ручные устройства;
- ручные линии;
- сохранение координат.

## Версия 0.4 — Monitoring

Добавить:

- Health Check;
- кнопки Start/Stop;
- schedule;
- per-device enable;
- события;
- Stale;
- monitoring status.

## Версия 0.5 — STP/RSTP/Rings

Добавить:

- bridge-port mapping;
- STP observations;
- Root Bridge;
- topology change;
- cycle detection;
- Physical Topology;
- Active STP Tree.

## Версия 0.6 — Incident Analysis

Добавить:

- topology snapshots;
- event correlation;
- MAC moves;
- MAC flapping;
- snapshot comparison.

---

# 81. Что НЕ входит в ранний MVP

Не реализовывать до появления объективной необходимости:

- сложные dashboards;
- универсальную систему графиков;
- мониторинг CPU всех возможных ОС;
- мониторинг SQL;
- VMware monitoring;
- Kubernetes;
- email engine уровня Zabbix;
- сложный web frontend;
- собственный scripting language;
- полноценный NMS.

Фокус продукта должен сохраняться.

---

# 82. Интеграция с Zabbix

В дальнейшем допускается интеграция.

Наш продукт не заменяет Zabbix.

Возможные интеграции:

- ссылка из устройства на соответствующий Zabbix Host;
- импорт списка hosts;
- экспорт обнаруженных устройств;
- отправка специализированных событий;
- отображение Zabbix trigger state.

Пример разделения ответственности:

```text
Zabbix:
CPU, memory, traffic, availability, alerts

Наш продукт:
physical topology, LLDP/CDP, rings, RSTP,
port-to-port, topology history
```

---

# 83. Критерии приёмки архитектуры

До начала крупной разработки должны выполняться следующие условия:

1. IP не является DeviceId.
2. Observation отделён от PhysicalLink.
3. FDB Observation не является PhysicalLink.
4. bridge-port не считается ifIndex.
5. UI не является частью Collector.
6. ручной элемент не удаляется Discovery.
7. stale data не удаляется автоматически.
8. monitoring можно полностью выключить.
9. Discovery имеет rate limits.
10. никакого credential guessing.
11. все пользовательские тексты русские.
12. все комментарии собственного кода русские.

---

# 84. Общие критерии качества

Для каждого релиза:

```text
Build = OK
Unit Tests = OK
Integration Tests = OK
Static checks = OK
DB migration test = OK
Simulation test = OK
```

Регрессия уже работающих функций не допускается.

---

# 85. Тестирование

Обязательные классы тестов:

## Unit tests

- parsers;
- normalization;
- MAC;
- OID;
- bridge-port mapping;
- identity resolver;
- topology resolver;
- cycle detection;
- event correlation.

## Simulation tests

Сохранённые реальные snapshots.

## Integration tests

SQLite + collectors.

## Hardware tests

По возможности:

- MOXA;
- MikroTik;
- Cisco.

## Failure tests

- timeout;
- partial SNMP;
- malformed response;
- device reboot;
- IP change;
- LLDP disappearance;
- duplicate identity;
- database unavailable.

---

# 86. Тест поиска колец

Должны существовать синтетические графы:

```text
A-B-C
```

ожидается:

```text
0 rings
```

```text
A-B-C-A
```

ожидается:

```text
1 ring
```

```text
A-B-C-A
  \ D /
```

алгоритм должен корректно работать с несколькими циклами.

Ручные устройства должны участвовать в этих тестах.

---

# 87. Тест RSTP

Необходимо отдельно тестировать ошибочное предположение:

```text
bridgePort == ifIndex
```

Создать snapshot, где:

```text
bridgePort = 3
ifIndex = 17
```

и убедиться, что STP state назначается Interface 17.

---

# 88. Git и версии БД

Каждая миграция получает номер.

Например:

```text
001_initial.sql
002_locations.sql
003_link_evidence.sql
```

SchemaVersion сохраняется в БД.

Не допускается silent schema modification.

---

# 89. Документация архитектурных решений

В репозитории рекомендуется создать:

```text
/docs/TECHNICAL_SPECIFICATION.md
/docs/ARCHITECTURE.md
/docs/AI_DEVELOPMENT_RULES.md
/docs/PROJECT_STATE.md
/docs/DECISIONS.md
```

`TECHNICAL_SPECIFICATION.md` содержит настоящее ТЗ.

`ARCHITECTURE.md` описывает фактическую архитектуру кода.

`PROJECT_STATE.md` содержит текущее состояние разработки.

`DECISIONS.md` является журналом архитектурных решений.

---

# 90. Регламент разработки с ИИ

Проект предполагается разрабатывать совместно с ИИ через чат.

ИИ обязан работать как старший разработчик, который изменяет существующий проект, а не как генератор абстрактных примеров.

Следующие правила являются обязательными.

---

# 91. Сначала изучить существующий код

Перед изменением существующего модуля ИИ обязан сначала получить или прочитать его фактическую текущую версию.

Запрещается строить изменения на предположении:

```text
Наверное, этот класс выглядит так...
```

Если доступ к репозиторию имеется, ИИ самостоятельно читает необходимые файлы.

Если доступа нет — запрашивается конкретный файл или diff только тогда, когда без него невозможно безопасно продолжить.

---

# 92. Не спрашивать то, что можно проверить

Если ответ можно получить:

- просмотром файла;
- поиском по проекту;
- просмотром схемы БД;
- grep;
- git diff;
- запуском тестов,

ИИ должен сначала выполнить проверку.

Не следует перекладывать эту работу на пользователя.

---

# 93. Сначала подтверждать факт, затем менять код

Пример правильного поведения:

```text
Подтверждено — таблица Links уже содержит SourcePortId и
TargetPortId. Поэтому миграция схемы для этой задачи не нужна.
Меняю только Resolver и UI.
```

Такой стиль является рекомендуемым.

---

# 94. Минимальные изменения

ИИ должен предпочитать минимальную локальную правку.

Нельзя выполнять большой refactoring только потому, что новая структура кажется красивее.

Перед refactoring должен существовать конкретный технический мотив.

---

# 95. Не создавать новую сущность без проверки существующей

Перед:

- новой таблицей;
- новым сервисом;
- новым DTO;
- новым endpoint;
- новой dependency

ИИ должен проверить, нельзя ли корректно использовать существующую структуру.

---

# 96. Разделять работу на проверяемые этапы

Например:

```text
Этап 1 — Collector
Этап 2 — проверка сборки
Этап 3 — Resolver
Этап 4 — тесты
Этап 5 — UI
```

Не следует одновременно переписывать:

- БД;
- Collector;
- Resolver;
- UI;

если изменения можно проверить последовательно.

---

# 97. Сначала backend, затем UI

Если функциональность требует изменения domain/backend и UI:

1. изменить backend;
2. собрать;
3. запустить тесты;
4. убедиться, что backend стабилен;
5. только затем менять UI.

Это уменьшает количество одновременно неизвестных проблем.

---

# 98. После логического шага — проверка

После изменения значимого блока ИИ должен запускать или давать пользователю точные команды проверки.

Например:

```powershell
dotnet / MSBuild ...
```

или конкретные команды тестов проекта.

Результат проверки анализируется до следующего крупного изменения.

---

# 99. Не угадывать ошибки компиляции

Если build завершился ошибкой, необходимо получить полный вывод.

После этого ИИ должен исправлять ошибки на основании фактического вывода.

Не следует заранее переписывать десятки файлов «на всякий случай».

---

# 100. Фиксировать просмотренные и изменённые файлы

После этапа ИИ должен кратко сообщать:

```text
Просмотрено:
...

Изменено:
...

Добавлено:
...

Удалено:
...
```

Это позволяет пользователю контролировать изменения.

---

# 101. Указывать абсолютные или репозиторные пути

При передаче файлов пользователю необходимо явно писать:

```text
internal/topology/resolver.cs
internal/snmp/stp_collector.cs
ui/map/NetworkMapControl.xaml
```

Не использовать формулировки:

```text
«в том файле»
«в сервисе»
```

без указания конкретного пути.

---

# 102. Diff перед commit

Перед крупным commit ИИ должен проверить diff.

Особенно:

- случайное форматирование;
- изменение line endings;
- удаление кода;
- изменение SQL;
- credential;
- лишние generated files.

---

# 103. Line endings и кодировка

Проект работает в Windows.

ИИ должен контролировать:

- CRLF;
- UTF-8;
- BOM, если это принято проектом.

Не следует без причины менять окончания строк всего существующего файла.

---

# 104. Комментарии ИИ

Все добавляемые ИИ комментарии в коде должны быть русскими.

Например:

```csharp
// Сохраняем исходное значение OID.
// Оно потребуется при разборе различий между прошивками MOXA.
```

Недопустимо:

```csharp
// Save original OID for debugging.
```

Исключение — текст официального протокола или название внешнего API.

---

# 105. UI от ИИ

Любой новый пользовательский текст, добавленный ИИ, должен быть на русском.

Например:

```text
Обновить топологию
Мониторинг остановлен
Последний опрос
Достоверность связи
```

---

# 106. Сохранение обратной совместимости

Перед изменением:

- БД;
- JSON;
- config;
- public API,

ИИ обязан проверить существующие потребители.

Если требуется breaking change, он должен быть явно описан.

---

# 107. Работа с БД

Перед созданием миграции ИИ обязан:

1. посмотреть реальную текущую схему;
2. проверить существующие таблицы;
3. определить, действительно ли нужна миграция;
4. показать причину изменения.

---

# 108. Работа с зависимостями

ИИ не должен автоматически устанавливать NuGet package только ради сокращения нескольких строк кода.

Для каждой dependency необходимо указать:

```text
зачем;
лицензия;
поддержка .NET Framework 4.8;
поддержка Windows 8.1/Server 2012;
чем хуже реализация без dependency.
```

---

# 109. Работа с legacy ОС

ИИ запрещается использовать API, которое фактически требует современного .NET или Windows, не проверив совместимость.

Целевой baseline:

```text
Windows 8.1
Windows Server 2012
.NET Framework 4.8
```

---

# 110. Не заменять рабочий модуль без необходимости

Если существующий код рабочий, предпочтительнее:

```text
маленькое расширение
```

чем:

```text
полное переписывание
```

---

# 111. Сначала тест, затем архитектурное усложнение

При спорном поведении предпочтительно добавить reproducible test/snapshot.

Например:

```text
MOXA firmware X:
bridgePort 3 → ifIndex 17
```

после чего исправлять алгоритм.

---

# 112. Работа с неопределённостью

Если данные устройства неоднозначны, ИИ не должен скрывать это.

Правильно:

```text
По LLDP удалённый chassis определён,
но remote port невозможно сопоставить с интерфейсом однозначно.
Сохраняю Observation как частичное.
```

Неправильно:

```text
Наверное это Port8, поэтому поставим Port8.
```

---

# 113. Никаких ложных фактов

Особенно запрещено придумывать:

- vendor OID;
- MIB;
- состояние порта;
- аппаратные возможности;
- номера интерфейсов.

Если значение не подтверждено:

```text
Unknown
```

является корректным результатом.

---

# 114. Стиль отчёта ИИ после изменения

Рекомендуемый формат:

```text
Подтверждено:
...

Сделано:
...

Файлы:
...

Проверка:
...

Осталось:
...
```

Отчёт должен быть коротким и техническим.

---

# 115. Пример отчёта ИИ

```text
Подтверждено — FDB возвращает bridge-port index,
а таблица Interfaces использует ifIndex.
Миграция БД не нужна: InterfaceId уже позволяет сохранить
результат после нормализации.

Сделано:

- добавлен BridgePortResolver;
- FDB Collector теперь проходит через
  dot1dBasePortIfIndex;
- сырые bridge-port значения сохраняются в Observation;
- добавлены два unit-теста, включая случай
  bridgePort=3 / ifIndex=17.

Изменено:

src/NetLoom.Snmp/BridgePortResolver.cs
src/NetLoom.Snmp/FdbCollector.cs
tests/NetLoom.Tests/BridgePortResolverTests.cs

Проверка:

Build — OK
Tests — 84/84 OK

Следующим отдельным шагом можно перевести STP Collector
на тот же BridgePortResolver.
```

---

# 116. Один commit — одна логическая задача

Не следует объединять в одном commit:

```text
RSTP mapping
+
редизайн UI
+
SQLite cleanup
+
переименование проекта
```

Предпочтительно:

```text
Commit 1:
Корректный bridge-port mapping

Commit 2:
Использование mapping в STP Collector

Commit 3:
Отображение STP state на карте
```

---

# 117. Commit messages

Commit messages должны быть понятными и желательно русскими.

Пример:

```text
RSTP: добавлено корректное сопоставление bridge-port с ifIndex
```

Для сложного commit допускается подробное описание.

---

# 118. Не выполнять commit до проверки

Последовательность:

```text
Изменение
↓
Build
↓
Tests
↓
Diff
↓
Commit
```

---

# 119. Не скрывать незавершённость

Если закончена только backend-часть, ИИ должен писать:

```text
Backend готов.
UI ещё не изменён.
```

а не:

```text
Функция готова.
```

---

# 120. PROJECT_STATE.md

После значимых этапов желательно обновлять:

```text
/docs/PROJECT_STATE.md
```

Минимальное содержимое:

```text
Текущая версия:
0.2

Последний завершённый этап:
LLDP Collector

Текущая схема БД:
7

Работает:
...

В разработке:
...

Известные проблемы:
...

Следующий шаг:
...
```

Это необходимо, поскольку разработка ведётся в длинных чатах и контекст может быть потерян.

---

# 121. DECISIONS.md

Архитектурные решения необходимо фиксировать.

Например:

```text
ADR-001
IP не является DeviceId.

ADR-002
FDB observation не является физической связью.

ADR-003
Существующие узлы автоматически не удаляются.

ADR-004
Автоматическая укладка не перемещает
зафиксированные пользователем узлы.
```

ИИ обязан учитывать эти решения в следующих изменениях.

---

# 122. Definition of Done

Задача считается завершённой только если:

- код реализован;
- build проходит;
- связанные tests проходят;
- ошибки обработаны;
- UI русифицирован;
- комментарии русские;
- миграция проверена, если она существует;
- существующий функционал не сломан;
- diff просмотрен;
- документация обновлена при изменении архитектуры.

---

# 123. Основная философия разработки

Система должна стремиться не к максимальному количеству получаемых данных, а к максимальной достоверности выводов.

Правильный ответ системы:

```text
Неизвестно
```

лучше неправильного:

```text
Port 3 подключён к Port 8
```

если фактических доказательств этого нет.

Система должна всегда разделять:

```text
Факт
Наблюдение
Предположение
Ручное утверждение оператора
Устаревшие данные
```

Это является фундаментальным требованием продукта.

---

# 124. Конечный результат разработки

Завершённая система должна позволять оператору открыть одну карту и сразу увидеть:

```text
Где находится оборудование.

Что с чем соединено.

Через какие порты.

Какой тип линии используется.

Какие данные подтверждены.

Какие данные предполагаются.

Какие элементы добавлены вручную.

Какие данные устарели.

Какие физические кольца существуют.

Кто является Root Bridge.

Где RSTP блокирует кольцо.

Что изменилось перед аварией.

Какие устройства сейчас недоступны.

Какие устройства просто не разрешено опрашивать.

Когда последний раз выполнялся мониторинг.
```

При этом система должна оставаться простым специализированным инструментом анализа сетевой топологии, а не превращаться в универсальную систему мониторинга уровня Zabbix.

# Архитектурное уточнение — cross-platform deployment

Этот раздел имеет приоритет над более ранними Windows-only формулировками ТЗ в части runtime, backend host и IPC.

- Shared core (`NetLoom.Domain`, `NetLoom.Application`, `NetLoom.Contracts`, `NetLoom.Topology`) — `netstandard2.0`.
- `NetLoom.Protocols.Snmp` и `NetLoom.Persistence.Sqlite` — `net48;net8.0`.
- `NetLoom.Service` и `NetLoom.Wpf` остаются `net48` для legacy Windows 8.1.
- `NetLoom.Engine` — modern Windows/Linux host; сейчас `net8.0`, целевой production runtime — .NET 10 LTS после обновления toolchain.
- WPF остаётся Windows-only клиентом. После service split UI не пишет SQLite напрямую.
- IPC определяется transport-neutral контрактами; Named Pipes — только локальная Windows-реализация транспорта.
- DPAPI остаётся Windows-реализацией `ISecretProtector`; Linux secret protector реализуется отдельно.
- Основной Engine остаётся на C#/.NET; Go допускается только как возможный будущий `NetLoom.Probe`.

## Topology Resolver v1

Система должна отделять наблюдения протоколов от подтверждённого факта физического соединения.

Topology Resolver формирует промежуточные `PhysicalLinkCandidate` с объяснимым набором evidence.

Требования:
- LLDP и CDP могут создавать strong adjacency evidence;
- FDB означает только наблюдение MAC за bridge-port;
- ARP/Neighbor Discovery связывает IP и MAC и является вспомогательным evidence;
- ARP/FDB correlation самостоятельно не создаёт PhysicalLink;
- IP-адрес не является внутренним DeviceId;
- protocol-specific port indexes должны сохранять исходную семантику;
- LLDP localPortNumber, CDP cdpCacheIfIndex, bridgePortIndex и IF-MIB ifIndex не должны неявно отождествляться;
- PhysicalLink создаётся только отдельным этапом materialization/lifecycle после topology resolution.
## Lifecycle и stale topology

Система должна отделять недоступность мониторинга от факта существования устройства или физической связи.

Требования:
- topology freshness имеет состояния Fresh, Aging и Stale;
- timeout не является основанием для удаления Device;
- poll failure не является основанием для удаления PhysicalLink;
- отсутствие нового LLDP/CDP/FDB/ARP evidence не удаляет существующую topology автоматически;
- Stale означает устаревшее подтверждение, а не отсутствие физического объекта;
- новое валидное evidence возвращает discovered topology в актуальное состояние;
- manual topology не удаляется и не переводится в stale discovery-процессом;
- FirstSeenUtc и LastSeenUtc должны сохранять историю наблюдаемости;
- LastSeenUtc не должен уменьшаться при обработке более старого observation;
- persistence lifecycle должен использовать стабильные внутренние идентификаторы materialized topology, а не IP-адрес или временный строковый ключ.
## Map v1

Система должна отображать физическую топологию через отдельную transport-neutral модель карты.

Требования:
- UI получает готовый MapSnapshot;
- topology resolution не выполняется внутри WPF;
- MapSnapshot содержит nodes, links, confidence, freshness и supporting evidence;
- presentation key узла не является DeviceId;
- IP-адрес не используется как внутренний DeviceId;
- одинаковая физическая связь не должна дублироваться только из-за противоположных LLDP/CDP observations;
- layout должен быть deterministic для одинакового набора topology данных;
- Map v1 не требует GraphX/MSAGL;
- интерфейс пользователя должен быть русскоязычным;
- map contracts должны быть пригодны для будущего transport-neutral IPC между UI и backend.
## Locations и физическое расположение

Система должна поддерживать отдельный иерархический справочник физических расположений.

Примеры уровней:
- площадка;
- здание;
- этаж;
- помещение;
- шкаф.

Требования:
- Location имеет внутренний GUID LocationId;
- Location может иметь ParentLocationId;
- циклическая иерархия запрещена;
- rename и move Location не изменяют DeviceId или PhysicalLink;
- Location не является частью device identity;
- sysLocation и CDP PhysicalLocation являются наблюдаемыми metadata и не должны автоматически назначать постоянный Location;
- WPF только отображает Location, полученный через transport-neutral map contracts;
- постоянная Device→Location assignment должна использовать внутренний DeviceId, а не IP или presentation key.
## Sprint 15 - materialized physical topology

Discovered and manually entered elements use one common physical graph.

Devices, interfaces and physical links use internal GUID identities. An IP address is not a DeviceId.

A manual unmanaged device uses DiscoveryOrigin = Manual and MonitoringCapability = None.

A manual interface uses ifIndex = null and IsManual = true.

A manual physical link uses PhysicalLinkStrength = Manual.

Automatic discovery must not delete or replace manual topology without an explicit user action.

Manual and automatic elements participate in the same topology analysis, including future cycle and ring detection.

Persistent Device-to-Location assignment uses internal DeviceId, not an IP address or a map presentation key.

A manual user action uses the common observation model: ObservationKind.Manual and SourceAddress = User.

## Sprint 15.1b — canonical PhysicalLink identity

Требования к materialized PhysicalLink:
- внутренний постоянный идентификатор — `PhysicalLink.Id`;
- `LinkKey` формируется Domain из canonical ordered endpoint'ов и не задаётся caller'ом;
- обратный порядок endpoint'ов должен формировать тот же `LinkKey`;
- однозначное refinement сохраняет `PhysicalLink.Id` и `FirstSeenUtc`;
- неоднозначное refinement не разрешается угадыванием;
- более слабое rediscovery не должно стирать уже определённый interface endpoint;
- automatic discovery не может заменить manual PhysicalLink;
- `FirstSeenUtc` сохраняет самое раннее известное значение;
- `LastSeenUtc` и `LastConfirmedUtc` сохраняют самые поздние известные значения;
- `TopologyDevice.LastResolvedUtc` сохраняет самое позднее известное значение.

## Sprint 15.1c — current PhysicalLink evidence

Materialized PhysicalLink должен предоставлять текущие evidence для объяснения происхождения физической связи.

Требования:
- evidence хранится отдельно от самого PhysicalLink;
- `SlotDiscriminator` обязателен;
- SourceAddress является частью identity evidence slot и сохраняет направление;
- LLDP/CDP используют directional local-port discriminator;
- ARP/FDB correlation использует MAC-based discriminator или детерминированный fallback;
- `ObservationId` и `CapturedUtc` остаются nullable;
- current evidence является bounded snapshot с replace/last-write-wins semantics;
- current evidence не используется как append-only history или time-series storage;
- automatic materialized MapLink получает current evidence из persistence;
- manual MapLink сохраняет synthetic Manual evidence.

## Sprint 16 — Live Map backend bridge

Рабочая карта должна получать `MapSnapshot` из materialized backend, а не создавать пустой snapshot внутри renderer.

Требования:
- WPF зависит от `IMapSnapshotProvider`, а не от SQLite/Topology implementations;
- provider читает devices, interfaces, PhysicalLink, current PhysicalLink evidence и Locations;
- `MaterializedTopologyMapProjector` остаётся единственным местом формирования materialized `MapSnapshot`;
- WPF периодически перечитывает snapshot и сохраняет последнее presentation-состояние только в UI;
- WPF не выполняет запись в topology/configuration SQLite;
- Windows composition root отвечает за concrete wiring и database initialization;
- путь БД может быть задан оператором через `--database` или `NETLOOM_DATABASE`;
- будущий service/IPC split должен сохранять provider boundary и не переносить topology business logic в WPF.

## Sprint 17 — Simulator raw SNMP replay

Simulator должен воспроизводить сохранённые raw SNMP observations через production parser path.

Требования:
- snapshot format версионируется;
- сохраняются observation metadata и полный набор raw varbind fields: OID, type code, display value, encoded bytes;
- encoded bytes сериализуются без потери данных;
- replay восстанавливает `SnmpObservation` и передаёт его production parser-у соответствующего ObservationKind;
- LLDP/CDP/FDB/ARP parser logic не дублируется в Simulator;
- FDB replay не трактует bridgePort как ifIndex;
- fixture snapshots не содержат credentials или production identifiers;
- unsupported kind завершается явной ошибкой, а не скрытым fallback;
- Inventory не считается поддержанным, пока production code не имеет raw inventory parser boundary.

## Sprint 18 — Monitoring Runtime

Monitoring Runtime реализует один polling cycle и не включает Scheduler.

Требования:
- runtime вызывает production LLDP/CDP/FDB/ARP collectors;
- один failed collector не прекращает остальные collectors cycle;
- raw observation сохраняется существующим collector-ом до normalized observation;
- runtime не создаёт direct PhysicalLink из FDB/ARP;
- bridgePort не трактуется как ifIndex;
- Engine не принимает SNMP secret material через command line;
- secret material не логируется;
- v1/v2/v3 credentials передаются operator-controlled environment;
- current topology/configuration SQLite не используется для high-frequency metrics;
- Scheduler, Health Monitoring и Interface Monitoring остаются независимыми этапами;
- после появления реального Engine polling linux-x64 acceptance включает фактический запуск Engine внутри Linux environment, а не только publish.

## Sprint 19 — Scheduler

Scheduler должен повторять существующий `MonitoringRuntime.PollOnce`, не дублируя protocol polling logic.

Требования:
- первый cycle запускается немедленно;
- cadence использует fixed-delay после завершения предыдущего cycle;
- overlap cycle одного scheduler instance запрещён by construction;
- cancellation предотвращает следующий cycle и прерывает interval wait;
- текущий synchronous in-flight poll не считается interruptible до появления cancellation-aware collector contracts;
- Engine предоставляет отдельную команду `schedule`;
- operator cadence задаётся `--interval-seconds`;
- `Ctrl+C` выполняет graceful stop;
- Scheduler не вводит Health/Interface metric ingestion;
- high-frequency metrics не записываются в topology/configuration SQLite;
- остановка Scheduler не удаляет последнее известное topology/observation state.

## Sprint 20 — metric/time-series storage boundary

Перед high-frequency Health/Interface monitoring должна существовать отдельная metric/time-series storage abstraction.

Требования:
- topology/configuration SQLite не используется как high-frequency metric history;
- metric sample требует стабильный `DeviceId`;
- IP-адрес не может подменять `DeviceId`;
- interface series в будущем использует `InterfaceId`, а не ifIndex как identity;
- unbound Health snapshot может существовать без `DeviceId`, но не сохраняется как persistent metric series;
- Health baseline model включает availability и uptime;
- concrete metric backend, retention и aggregation выбираются отдельно;
- Sprint 20 не реализует Interface monitoring и не закрывает backlog Health monitoring.

## Sprint 21 — Health monitoring

Health monitoring должен использовать лёгкий отдельный SNMP poll и существующий Scheduler.

Требования:
- production source — `sysUpTime.0`;
- Health poll не должен запускать полную interface inventory collection;
- успешный SNMP GET даёт current Health `Up`;
- uptime парсится при поддерживаемом representation, иначе остаётся unknown/null;
- timeout/socket/protocol/credential failure не считается доказательством отсутствия Device;
- IP не используется как DeviceId;
- stable `DeviceId` передаётся отдельно и может отсутствовать для unbound live result;
- Health failure изолирован от остальных protocol steps cycle;
- отдельный Health scheduler implementation не создаётся;
- `schedule --kinds health` использует общий fixed-delay Scheduler;
- high-frequency Health history не пишется в topology/configuration SQLite;
- Interface monitoring не входит в Sprint 21.
